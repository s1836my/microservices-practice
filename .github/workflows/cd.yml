name: CD

on:
  push:
    branches: [main]
    tags:
      - 'v*'

env:
  GO_VERSION: "1.22"
  REGISTRY: ghcr.io
  IMAGE_PREFIX: ${{ github.repository_owner }}/micromart

jobs:
  # ── Docker イメージをビルド & GHCR にプッシュ ──────────────────────────────
  build-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    strategy:
      matrix:
        service: [gateway, user, product, search, cart, order, payment, notification]
    outputs:
      image-tag: ${{ steps.meta.outputs.tags }}
    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Docker metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}-${{ matrix.service }}
          tags: |
            type=ref,event=branch
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=sha,prefix=sha-,format=short

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: services/${{ matrix.service }}
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          build-args: |
            VERSION=${{ github.ref_name }}
            COMMIT=${{ github.sha }}

  # ── K8s マニフェストの image タグを更新してデプロイ ──────────────────────
  # ※ 本番 K8s クラスタが存在する場合のみ有効化
  # deploy:
  #   runs-on: ubuntu-latest
  #   needs: build-push
  #   environment: production
  #   steps:
  #     - uses: actions/checkout@v4
  #
  #     - name: Set up kubectl
  #       uses: azure/setup-kubectl@v3
  #
  #     - name: Configure kubeconfig
  #       run: echo "${{ secrets.KUBECONFIG }}" | base64 -d > $HOME/.kube/config
  #
  #     - name: Update image tags
  #       run: |
  #         TAG="${{ github.sha }}"
  #         for svc in gateway user product search cart order payment notification; do
  #           kubectl set image deployment/$svc \
  #             $svc=${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}-$svc:sha-${TAG:0:7} \
  #             -n micromart
  #         done
  #
  #     - name: Wait for rollout
  #       run: |
  #         for svc in gateway user product search cart order payment notification; do
  #           kubectl rollout status deployment/$svc -n micromart --timeout=120s
  #         done
