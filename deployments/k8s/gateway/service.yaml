apiVersion: v1
kind: Service
metadata:
  name: gateway
  namespace: micromart
  labels:
    app: gateway
spec:
  selector:
    app: gateway
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP
